# Slack Incoming Webhook URL (Slack → your app → Incoming Webhooks → Add)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# HTTP mode only:
MCP_HTTP_PORT=7800
# Optional bearer token required by remote HTTP clients
MCP_AUTH_TOKEN=
